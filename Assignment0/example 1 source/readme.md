# ESA-VIS

Assignments for the Vision course using OpenCV.  
  
This will output 2 windows:
* The original image
* The inverted image

## Running Assignment

* run `cmake .`
* run `make`
* run `./cppInterface.out`